"""Agent panels.

`base.AgentPanel` holds what every panel repeats. The panels themselves are
still methods on `GodAI` (`build_author_panel()` and friends); phase 4 moves
each one into its own module here.
"""
